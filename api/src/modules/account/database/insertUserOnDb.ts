import { Result } from '../../utils/types';
import User from '../types/User';
import UserInput from '../types/UserInput';

export default function insertUserOnDb(user: UserInput): Result<string | User> {

}
